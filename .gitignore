node_modules/
dist/
build/
.react-router/
.env
.env.*
!.env.example
coverage/
*.log
.DS_Store
/tmp/

# Local build-verification harness: this container intercepts TLS, so a Docker build here
# needs its CA bundle in the context. It is never committed and never enters an image.
.ci-ca.crt
